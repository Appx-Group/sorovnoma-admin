export type SingleChannelType = {
    id: string,
    name: string,
    link: string,
    createdAt: string,
    updatedAt: string
}

export type CreateOrUpdateChannelType = {
    id: string,
    name: string,
    link: string,
}